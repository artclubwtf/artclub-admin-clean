"use client";
import {createNetworkApiClient} from "@artclub/api-client";
import Link from "next/link";
import {useEffect,useState} from "react";
const api=createNetworkApiClient();
export function ProfileEvents({profileId,mine}:{profileId?:string;mine:boolean}){const[events,setEvents]=useState<any[]>([]);useEffect(()=>{if(!profileId)return;api.request<any>(`/events?when=all&profileId=${encodeURIComponent(profileId)}${mine?"&scope=mine":""}`).then(data=>setEvents(data.events)).catch(()=>undefined)},[profileId,mine]);if(!events.length)return null;return <section className="mx-auto max-w-4xl px-5 pb-16"><div className="mb-4 flex items-center justify-between"><h2 className="text-2xl font-semibold">Events</h2>{mine&&<Link href="/events/new" className="text-sm underline">Create event</Link>}</div><div className="grid gap-4 sm:grid-cols-2">{events.map(event=><Link key={event.id} href={`/events/${event.id}`} className="overflow-hidden rounded-2xl border dark:border-neutral-800">{event.coverImageUrl&&<img src={event.coverImageUrl} alt="" className="aspect-[16/8] w-full object-cover"/>}<div className="p-4"><p className="text-sm text-neutral-500">{new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short",timeZone:event.timezone}).format(new Date(event.startAt))}</p><h3 className="mt-1 text-lg font-medium">{event.title}</h3>{event.status!=="published"&&<span className="text-xs capitalize text-neutral-500">{event.status}</span>}</div></Link>)}</div></section>}
