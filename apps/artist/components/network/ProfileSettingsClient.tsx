"use client";

import { createNetworkApiClient } from "@artclub/api-client";
import { FormEvent, useState } from "react";
import { ImageUpload, type ImageUploadItem } from "@/components/forms/ImageUpload";

const api=createNetworkApiClient();const accountTypes=[["artist","Artist"],["art_enthusiast","Collector / Art Enthusiast"],["gallery","Gallery"],["event_series","Event Organizer"],["curator","Curator"],["institution","Institution"]];
export function ProfileSettingsClient({initial}:{initial:any}){
  const[value,setValue]=useState({...initial,disciplines:(initial.disciplines||[]).join(", "),interests:(initial.interests||[]).join(", "),shopEnabled:initial.shopEnabled===true});const[status,setStatus]=useState("");const[uploadBusy,setUploadBusy]=useState(false);const fixedArtist=value.profileType==="artist"&&value.profileTypeSource==="existing_artist_link";
  const avatarItems:ImageUploadItem[]=value.profileImageUrl?[{url:value.profileImageUrl,storageKey:value.profileImageStorageKey}]:[];const coverItems:ImageUploadItem[]=value.coverImageUrl?[{url:value.coverImageUrl,storageKey:value.coverImageStorageKey}]:[];
  async function submit(event:FormEvent){event.preventDefault();if(uploadBusy)return;setStatus("Saving…");try{const body={profileType:value.profileType,displayName:value.displayName,username:value.username,bio:value.bio,city:value.city,country:value.country,disciplines:String(value.disciplines||"").split(",").map(item=>item.trim()).filter(Boolean),interests:String(value.interests||"").split(",").map(item=>item.trim()).filter(Boolean),website:value.website,instagram:value.instagram,profileImageUrl:value.profileImageUrl,coverImageUrl:value.coverImageUrl,isPublic:value.isPublic,shopEnabled:value.profileType==="artist"?value.shopEnabled:undefined};const data=await api.request<any>("/profile",{method:"PATCH",body:JSON.stringify(body)});if(data.next){location.href=data.next;return}setValue((current:any)=>({...current,...data.profile,disciplines:(data.profile.disciplines||[]).join(", "),interests:(data.profile.interests||[]).join(", ")}));setStatus("Saved")}catch{setStatus("Could not save changes.")}}
  const field=(key:string,label:string)=><label className="block"><span className="mb-2 block text-sm font-medium">{label}</span><input disabled={uploadBusy} value={value[key]||""} onChange={event=>setValue({...value,[key]:event.target.value})} className="control w-full"/></label>;
  const shopStatus=value.shopStatus==="ready"?"Active in shop":value.shopStatus==="pending"?"Shop activation queued":value.shopStatus==="setup_required"?"Shopify setup required":value.shopStatus==="error"?"Shop setup needs attention":"Not active in shop";
  return <form onSubmit={submit} className="app-page max-w-3xl"><h1 className="page-heading">Profile</h1><div className="mt-8 space-y-9"><section className="space-y-5"><h2 className="eyebrow">Account</h2><label className="block"><span className="mb-2 block text-sm font-medium">Account type</span><select disabled={fixedArtist||uploadBusy} value={value.profileType} onChange={event=>setValue({...value,profileType:event.target.value})} className="control w-full disabled:opacity-60">{accountTypes.map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></label>{field("displayName","Display name")}{field("username","Username")}</section><section className="space-y-5"><h2 className="eyebrow">About</h2><div className="grid gap-5 sm:grid-cols-2">{field("city","City")}{field("country","Country")}</div><label className="block"><span className="mb-2 block text-sm font-medium">Bio</span><textarea disabled={uploadBusy} value={value.bio||""} onChange={event=>setValue({...value,bio:event.target.value})} rows={6} className="control w-full resize-y"/></label>{field("disciplines","Disciplines (comma separated)")}{field("interests","Interests (comma separated)")}</section><section className="space-y-5"><h2 className="eyebrow">Links & media</h2>{field("website","Website")}{field("instagram","Instagram")}<ImageUpload label="Profile image" variant="avatar" items={avatarItems} onChange={items=>setValue((current:any)=>({...current,profileImageUrl:items[0]?.url||"",profileImageStorageKey:items[0]?.storageKey||""}))} onUploadingChange={setUploadBusy}/><ImageUpload label="Cover image" variant="profile-cover" items={coverItems} onChange={items=>setValue((current:any)=>({...current,coverImageUrl:items[0]?.url||"",coverImageStorageKey:items[0]?.storageKey||""}))} onUploadingChange={setUploadBusy}/></section><label className="list-row"><span className="flex-1 font-medium">Visible profile</span><input type="checkbox" disabled={uploadBusy} checked={value.isPublic!==false} onChange={event=>setValue({...value,isPublic:event.target.checked})}/></label>{value.profileType==="artist"?<label className="flex items-start gap-3 rounded-xl bg-[var(--surface-muted)] p-4"><input type="checkbox" disabled={uploadBusy} checked={value.shopEnabled===true} onChange={event=>setValue({...value,shopEnabled:event.target.checked})} className="mt-1"/><span><span className="block font-medium">Show my artist profile and artworks in the ARTCLUB Shop</span><span className="meta-text mt-1 block">The Network profile remains available independently. Shop activation uses the linked canonical artist and Shopify sync.</span><span className={`mt-2 block text-xs font-medium ${value.shopStatus==="setup_required"||value.shopStatus==="error"?"text-[var(--danger)]":"text-[var(--text-muted)]"}`}>{shopStatus}</span></span></label>:null}<div className="flex items-center gap-4"><button disabled={uploadBusy} className="primary-action disabled:opacity-40">{uploadBusy?"Uploading…":"Save changes"}</button><span className="meta-text">{status}</span></div></div></form>;
}
