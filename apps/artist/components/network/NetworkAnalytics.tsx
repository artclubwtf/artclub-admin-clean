"use client";import { createNetworkApiClient } from "@artclub/api-client";import { useEffect,useState } from "react";const api=createNetworkApiClient();
export function NetworkAnalytics(){const[data,setData]=useState<any>();const[error,setError]=useState("");useEffect(()=>{api.request<any>("/analytics/me?days=30").then(setData).catch(e=>setError(e.message))},[]);if(error)return <p className="py-10 text-red-600">{error}</p>;if(!data)return <p className="py-10 text-neutral-400">Loading analytics…</p>;const m=data.metrics;const values=[...[ ["Profile views",m.profileViews],["Unique visitors",m.uniqueProfileVisitors],["Post impressions",m.postImpressions],["Post engagement",m.postEngagements],["Connections",m.connections],["Event views",m.eventViews],["Event RSVPs",m.eventRsvps],["Shop clicks",m.shopClicks],["Engagement rate",`${m.engagementRate}%`] ]];return <div className="mx-auto max-w-4xl py-7"><p className="text-sm uppercase tracking-widest text-neutral-400">Last 30 days</p><h1 className="text-3xl font-semibold">Network analytics</h1><div className="mt-7 grid grid-cols-2 gap-px overflow-hidden rounded-2xl bg-neutral-200 sm:grid-cols-3 dark:bg-neutral-800">{values.map(([label,value])=><div key={label} className="bg-white p-5 dark:bg-neutral-950"><p className="text-2xl font-semibold">{value}</p><p className="mt-1 text-sm text-neutral-500">{label}</p></div>)}</div><p className="mt-5 text-xs text-neutral-500">Engagement: {m.engagementDefinition}</p></div>}
