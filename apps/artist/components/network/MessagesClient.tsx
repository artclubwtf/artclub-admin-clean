"use client";
import {createNetworkApiClient} from "@artclub/api-client";
import Link from "next/link";
import {FormEvent,useCallback,useEffect,useRef,useState} from "react";
const api=createNetworkApiClient();
function Avatar({participant}:{participant:any}){return participant?.profileImageUrl?<img src={participant.profileImageUrl} alt="" className="h-12 w-12 shrink-0 rounded-full object-cover"/>:<span className="grid h-12 w-12 shrink-0 place-items-center rounded-full bg-[var(--surface-soft)] font-medium">{participant?.displayName?.[0]||"·"}</span>}
export function InboxClient(){const[items,setItems]=useState<any[]>([]);const[q,setQ]=useState("");const[error,setError]=useState("");const load=useCallback(async()=>{try{const data=await api.request<any>(`/conversations?q=${encodeURIComponent(q)}`);setItems(data.conversations);setError("")}catch{setError("Conversations could not be loaded.")}},[q]);useEffect(()=>{void load()},[load]);return <main className="app-page max-w-6xl"><header><h1 className="page-heading">Messages</h1><p className="meta-text mt-2">Private conversations with your network.</p></header><div className="mt-7 overflow-hidden lg:grid lg:min-h-[36rem] lg:grid-cols-[24rem_1fr] lg:border-t lg:border-[var(--divider)]"><section className="lg:border-r lg:border-[var(--divider)] lg:pr-7"><input value={q} onChange={event=>setQ(event.target.value)} placeholder="Search conversations" className="control my-5 w-full"/>{error&&<p className="text-sm text-[var(--danger)]">{error}</p>}<div className="border-t border-[var(--divider)] lg:border-t-0">{items.map(item=><Link key={item.id} href={`/messages/${item.id}`} className="list-row"><Avatar participant={item.participant}/><div className="min-w-0 flex-1"><div className="flex items-baseline justify-between gap-3"><p className="truncate font-medium">{item.participant?.displayName}</p>{item.updatedAt&&<time className="text-xs text-[var(--text-faint)]">{new Intl.DateTimeFormat(undefined,{month:"short",day:"numeric"}).format(new Date(item.updatedAt))}</time>}</div><p className="meta-text truncate">{item.lastMessagePreview||"Start a conversation"}</p></div>{item.unreadCount>0&&<span className="grid min-h-5 min-w-5 place-items-center rounded-full bg-[var(--accent)] px-1 text-[11px] text-[var(--accent-text)]">{item.unreadCount}</span>}</Link>)}</div>{!items.length&&!error&&<p className="py-16 text-center text-[var(--text-faint)]">Your conversations will appear here.</p>}</section><aside className="hidden place-items-center lg:grid"><div className="max-w-xs text-center"><p className="section-heading">Select a conversation</p><p className="meta-text mt-2">Choose a person on the left to open your messages.</p></div></aside></div></main>}
export function ThreadClient({id}:{id:string}){const[items,setItems]=useState<any[]>([]);const[text,setText]=useState("");const[media,setMedia]=useState<any[]>([]);const[error,setError]=useState("");const file=useRef<HTMLInputElement>(null);const load=useCallback(async()=>{try{const data=await api.request<any>(`/conversations/${id}/messages`);setItems(data.messages);await api.request(`/conversations/${id}/read`,{method:"POST"});setError("")}catch{setError("Messages could not be loaded.")}},[id]);useEffect(()=>{void load();const timer=setInterval(()=>void load(),10000);return()=>clearInterval(timer)},[load]);async function upload(value:File){const form=new FormData();form.append("file",value);try{const data=await api.request<any>("/upload",{method:"POST",body:form});setMedia([data.media])}catch{setError("The attachment could not be uploaded.")}}async function send(event:FormEvent){event.preventDefault();try{const data=await api.request<any>(`/conversations/${id}/messages`,{method:"POST",body:JSON.stringify({text,media})});setItems(value=>[...value,data.message]);setText("");setMedia([])}catch{setError("The message could not be sent.")}}return <main className="app-page flex min-h-[calc(100vh-7rem)] max-w-4xl flex-col"><header className="border-b border-[var(--divider)] pb-4"><Link href="/messages" className="text-action">← Messages</Link></header><div className="flex-1 space-y-3 overflow-y-auto py-6">{items.map(item=><div key={item.id} className={`flex ${item.mine?"justify-end":"justify-start"}`}><div className={`max-w-[82%] rounded-2xl px-4 py-2.5 sm:max-w-[65%] ${item.mine?"bg-[var(--accent)] text-[var(--accent-text)]":"bg-[var(--surface-soft)]"}`}>{item.text&&<p className="whitespace-pre-wrap">{item.text}</p>}{item.media?.map((medium:any,index:number)=>medium.type==="image"?<img key={index} src={medium.url} alt="Message attachment" className="mt-2 max-h-72 rounded-xl"/>:<video key={index} src={medium.url} controls className="mt-2 max-h-72 rounded-xl"/>)}</div></div>)}</div>{error&&<p className="text-sm text-[var(--danger)]">{error}</p>}<form onSubmit={send} className="sticky bottom-24 mt-4 flex items-center gap-2 rounded-2xl bg-[var(--surface-soft)] p-2 lg:bottom-4"><input ref={file} type="file" accept="image/*" className="hidden" onChange={event=>event.target.files?.[0]&&void upload(event.target.files[0])}/><button type="button" onClick={()=>file.current?.click()} className="grid h-10 w-10 place-items-center rounded-full text-xl text-[var(--text-muted)]" aria-label="Attach image">+</button><input value={text} onChange={event=>setText(event.target.value)} placeholder={media.length?"Image ready":"Message"} className="min-w-0 flex-1 bg-transparent px-2 outline-none"/><button disabled={!text.trim()&&!media.length} className="primary-action disabled:opacity-40">Send</button></form></main>}
