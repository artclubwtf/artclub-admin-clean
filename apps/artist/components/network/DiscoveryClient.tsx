"use client";
import {createNetworkApiClient} from "@artclub/api-client";
import Link from "next/link";
import {FormEvent,useCallback,useEffect,useState} from "react";
const api=createNetworkApiClient();type Profile={id:string;slug:string;displayName:string;username:string;profileType:string;city:string;profileImageUrl:string};
function Avatar({profile}:{profile:any}){return profile?.profileImageUrl?<img src={profile.profileImageUrl} alt="" className="h-12 w-12 shrink-0 rounded-full object-cover"/>:<span className="grid h-12 w-12 shrink-0 place-items-center rounded-full bg-[var(--surface-soft)] font-medium">{profile?.displayName?.[0]||"·"}</span>}
export function DiscoveryClient(){const[q,setQ]=useState("");const[type,setType]=useState("");const[view,setView]=useState("suggested");const[profiles,setProfiles]=useState<Profile[]>([]);const[requests,setRequests]=useState<any[]>([]);const[error,setError]=useState("");const[loading,setLoading]=useState(true);const load=useCallback(async()=>{setLoading(true);try{if(view==="requests"||view==="connections"){const data=await api.request<any>(`/connections?status=${view==="requests"?"pending":"accepted"}`);setRequests(data.connections)}else{const data=await api.request<any>(`/profiles?q=${encodeURIComponent(q)}&type=${type}`);setProfiles(data.profiles)}setError("")}catch{setError("The network could not be loaded.")}finally{setLoading(false)}},[view,q,type]);useEffect(()=>{void load()},[view]);async function connect(id:string){try{await api.request("/connections",{method:"POST",body:JSON.stringify({profileId:id})});setProfiles(value=>value.filter(profile=>profile.id!==id))}catch{setError("The connection request could not be sent.")}}async function respond(id:string,action:string){try{await api.request(`/connections/${id}`,{method:"PATCH",body:JSON.stringify({action})});setRequests(value=>value.filter(item=>item.id!==id))}catch{setError("The request could not be updated.")}}return <main className="app-page max-w-4xl"><header><h1 className="page-heading">Network</h1><p className="meta-text mt-2">Discover people and organizations across the art world.</p></header><nav className="quiet-tabs mt-7">{[["suggested","Suggested"],["requests","Requests"],["connections","Connections"]].map(([key,label])=><button key={key} onClick={()=>setView(key)} className="quiet-tab" aria-selected={view===key}>{label}</button>)}</nav>{view==="suggested"&&<form onSubmit={(event:FormEvent)=>{event.preventDefault();void load()}} className="my-6 grid gap-3 sm:grid-cols-[1fr_auto_auto]"><input value={q} onChange={event=>setQ(event.target.value)} placeholder="Name, username, discipline…" className="control min-w-0"/><select value={type} onChange={event=>setType(event.target.value)} className="control"><option value="">All roles</option><option value="artist">Artists</option><option value="art_enthusiast">Collectors</option><option value="gallery">Galleries</option><option value="event_series">Event organizers</option><option value="curator">Curators</option><option value="institution">Institutions</option></select><button className="primary-action">Search</button></form>}{error&&<p className="mt-4 text-sm text-[var(--danger)]">{error}</p>}<div className="mt-2 border-t border-[var(--divider)]">{loading?[1,2,3,4].map(item=><div key={item} className="list-row animate-pulse"><span className="h-12 w-12 rounded-full bg-[var(--surface-soft)]"/><span className="h-4 w-48 rounded bg-[var(--surface-soft)]"/></div>):view==="suggested"?profiles.map(profile=><article key={profile.id} className="list-row"><Avatar profile={profile}/><div className="min-w-0 flex-1"><Link href={`/profile/${profile.slug}`} className="font-medium">{profile.displayName}</Link><p className="meta-text truncate">@{profile.username} · {profile.profileType.replaceAll("_"," ")}{profile.city?` · ${profile.city}`:""}</p></div><button onClick={()=>void connect(profile.id)} className="secondary-action">Connect</button></article>):requests.map(item=><article key={item.id} className="list-row"><Avatar profile={item.profile}/><div className="min-w-0 flex-1"><Link href={`/profile/${item.profile?.slug}`} className="font-medium">{item.profile?.displayName}</Link><p className="meta-text">{item.state.replaceAll("_"," ")}</p></div>{item.incoming&&item.state==="incoming_pending"?<div className="flex gap-2"><button onClick={()=>void respond(item.id,"accept")} className="primary-action">Accept</button><button onClick={()=>void respond(item.id,"decline")} className="text-action">Decline</button></div>:item.state==="connected"?<span className="meta-text">Connected</span>:null}</article>)}</div>{!loading&&!(view==="suggested"?profiles:requests).length&&!error&&<p className="py-16 text-center text-[var(--text-faint)]">Nothing to show yet.</p>}</main>}
