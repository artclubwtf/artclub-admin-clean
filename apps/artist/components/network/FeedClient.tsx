"use client";
import {createNetworkApiClient} from "@artclub/api-client";
import Link from "next/link";
import {FormEvent,useCallback,useEffect,useRef,useState} from "react";
import {useSearchParams} from "next/navigation";
import {trackNetwork} from "@/lib/client/network-analytics";
import {EventFeedCard} from "@/components/network/EventFeedCard";
const api=createNetworkApiClient();
type Post={id:string;text:string;media:Array<{url:string;type:"image"|"video"}>;likeCount:number;commentCount:number;liked:boolean;saved:boolean;mine:boolean;createdAt:string;author?:{slug:string;displayName:string;username:string;profileImageUrl:string}};
type Artwork={id:string;title:string;imageUrl:string;offering:string;priceLabel:string;shopUrl:string;liked:boolean;saved:boolean;likeCount:number};
type Item={kind:"post";id:string;sortDate:string;post:Post}|{kind:"artwork";id:string;sortDate:string;artwork:Artwork;artist:any}|{kind:"event";id:string;sortDate:string;event:any};
export function FeedClient({createOnly=false}:{createOnly?:boolean}){const params=useSearchParams();const[items,setItems]=useState<Item[]>([]);const[text,setText]=useState("");const[media,setMedia]=useState<Post["media"]>([]);const[filter,setFilter]=useState("all");const[busy,setBusy]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const[next,setNext]=useState<string|null>(null);const fileRef=useRef<HTMLInputElement>(null);
const load=useCallback(async(cursor?:string)=>{setLoading(true);try{setError("");const data=await api.request<{items:Item[];nextCursor:string|null}>(`/feed?filter=${filter}${cursor?`&cursor=${encodeURIComponent(cursor)}`:""}`);setItems(value=>cursor?[...value,...data.items]:data.items);setNext(data.nextCursor);for(const item of data.items)if(item.kind!=="event")trackNetwork(item.kind==="artwork"?"artwork_feed_impression":"post_impression",item.kind==="artwork"?{canonicalProductId:item.artwork.id}:{postId:item.post.id})}catch(reason:any){setError(reason.message)}finally{setLoading(false)}},[filter]);useEffect(()=>{if(!createOnly){void load();trackNetwork("feed_view")}},[load,createOnly]);
async function upload(files:FileList){setBusy(true);try{for(const file of Array.from(files)){const form=new FormData();form.append("file",file);const data=await api.request<any>("/upload",{method:"POST",body:form});setMedia(value=>[...value,data.media])}}catch(reason:any){setError(reason.message)}finally{setBusy(false)}}
async function submit(event:FormEvent){event.preventDefault();setBusy(true);try{trackNetwork("post_create_started");const type=params.get("type")==="process"?"process":media[0]?.type||"text";const data=await api.request<{item:Item}>("/feed",{method:"POST",body:JSON.stringify({type,text,media,visibility:"public"})});if(data.item.kind==="post")trackNetwork("post_created",{postId:data.item.post.id});setText("");setMedia([]);setItems(value=>[data.item,...value]);if(createOnly)location.href="/feed"}catch(reason:any){setError(reason.message)}finally{setBusy(false)}}
async function postAction(post:Post,key:"like"|"save"){const active=key==="like"?post.liked:post.saved;await api.request(`/posts/${post.id}/${key}`,{method:active?"DELETE":"POST"});trackNetwork("post_engagement",{postId:post.id});setItems(value=>value.map(item=>item.kind==="post"&&item.post.id===post.id?{...item,post:{...item.post,[key==="like"?"liked":"saved"]:!active,...(key==="like"?{likeCount:Math.max(0,item.post.likeCount+(active?-1:1))}:{})}}:item))}
async function artworkAction(artwork:Artwork,key:"like"|"save"){const active=key==="like"?artwork.liked:artwork.saved;await api.request(`/artworks/${artwork.id}/${key}`,{method:active?"DELETE":"POST"});setItems(value=>value.map(item=>item.kind==="artwork"&&item.artwork.id===artwork.id?{...item,artwork:{...item.artwork,[key==="like"?"liked":"saved"]:!active,...(key==="like"?{likeCount:Math.max(0,item.artwork.likeCount+(active?-1:1))}:{})}}:item))}
async function comment(post:Post){const value=prompt("Comment");if(!value)return;await api.request(`/posts/${post.id}/comments`,{method:"POST",body:JSON.stringify({text:value})});setItems(items=>items.map(item=>item.kind==="post"&&item.post.id===post.id?{...item,post:{...item.post,commentCount:item.post.commentCount+1}}:item))}
async function edit(post:Post){const value=prompt("Edit post",post.text);if(value===null)return;const data=await api.request<{post:Post}>(`/posts/${post.id}`,{method:"PATCH",body:JSON.stringify({text:value})});setItems(items=>items.map(item=>item.kind==="post"&&item.post.id===post.id?{...item,post:{...item.post,...data.post}}:item))}
async function remove(post:Post){if(!confirm("Delete this post?"))return;await api.request(`/posts/${post.id}`,{method:"DELETE"});setItems(items=>items.filter(item=>item.kind!=="post"||item.post.id!==post.id))}
return <div className="mx-auto max-w-2xl space-y-5 py-5">{!createOnly&&<><header><h1 className="text-3xl font-semibold">Feed</h1><p className="mt-1 text-neutral-500">Art, ideas and events from the ARTCLUB network.</p></header><nav className="flex gap-5 overflow-x-auto border-b text-sm dark:border-neutral-800">{[["all","All"],["posts","Posts"],["artworks","Artworks"],["events","Events"],["following","Following"]].map(([key,label])=><button key={key} className={`whitespace-nowrap pb-3 ${filter===key?"border-b border-current font-medium":"text-neutral-400"}`} onClick={()=>setFilter(key)}>{label}</button>)}</nav></>}
<form onSubmit={submit} className="space-y-3 rounded-2xl border p-4 dark:border-neutral-800"><textarea value={text} onChange={e=>setText(e.target.value)} rows={createOnly?8:3} maxLength={5000} placeholder={params.get("type")==="process"?"Show your process…":"Share something with the art world…"} className="w-full resize-none bg-transparent text-lg outline-none"/>{media.length>0&&<div className="grid grid-cols-2 gap-2">{media.map((item,index)=>item.type==="image"?<img key={index} src={item.url} alt="Upload preview" className="max-h-72 w-full rounded-2xl object-cover"/>:<video key={index} src={item.url} controls className="max-h-72 w-full rounded-2xl"/>)}</div>}<div className="flex justify-between"><input ref={fileRef} multiple className="hidden" type="file" accept="image/jpeg,image/png,image/webp,image/gif,video/mp4,video/webm,video/quicktime" onChange={e=>e.target.files&&void upload(e.target.files)}/><button type="button" onClick={()=>fileRef.current?.click()} className="rounded-full border px-4 py-2 text-sm">Add media</button><button disabled={busy||(!text.trim()&&!media.length)} className="rounded-full bg-neutral-950 px-5 py-2 text-sm text-white disabled:opacity-40 dark:bg-white dark:text-black">{busy?"Working…":"Post"}</button></div></form>
{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}{!createOnly&&loading&&!items.length&&<div className="space-y-4">{[1,2,3].map(value=><div key={value} className="h-72 animate-pulse rounded-3xl bg-neutral-100 dark:bg-neutral-900"/>)}</div>}{!createOnly&&!loading&&!items.length&&!error&&<p className="py-16 text-center text-neutral-500">Nothing here yet.</p>}
{!createOnly&&items.map(item=>item.kind==="event"?<EventFeedCard key={item.id} initial={item.event}/>:item.kind==="post"?<article key={item.id} className="rounded-3xl border p-5 dark:border-neutral-800"><header className="flex items-center gap-3">{item.post.author?.profileImageUrl?<img src={item.post.author.profileImageUrl} alt="" className="h-10 w-10 rounded-full object-cover"/>:<span className="h-10 w-10 rounded-full bg-neutral-200"/>}<div><Link href={`/profile/${item.post.author?.slug}`} className="font-medium">{item.post.author?.displayName}</Link><p className="text-xs text-neutral-400">@{item.post.author?.username}</p></div></header>{item.post.text&&<p className="whitespace-pre-wrap py-4 leading-7">{item.post.text}</p>}{item.post.media.map((medium,index)=>medium.type==="image"?<img key={index} src={medium.url} alt="Post media" className="max-h-[42rem] w-full rounded-2xl object-cover"/>:<video key={index} src={medium.url} controls className="max-h-[42rem] w-full rounded-2xl"/>)}<footer className="mt-4 flex flex-wrap gap-5 text-sm text-neutral-500"><button onClick={()=>void postAction(item.post,"like")}>{item.post.liked?"Liked":"Like"} {item.post.likeCount}</button><button onClick={()=>void comment(item.post)}>Comment {item.post.commentCount}</button><button onClick={()=>void postAction(item.post,"save")}>{item.post.saved?"Saved":"Save"}</button><button onClick={()=>navigator.share?.({url:`${location.origin}/feed?post=${item.post.id}`})}>Share</button>{item.post.mine&&<><button onClick={()=>void edit(item.post)}>Edit</button><button onClick={()=>void remove(item.post)} className="text-red-600">Delete</button></>}</footer></article>:<article key={item.id} className="rounded-3xl border p-5 dark:border-neutral-800"><header className="mb-3 flex items-center justify-between"><Link href={`/profile/${item.artist.slug}`} className="font-medium">{item.artist.displayName}</Link><span className="text-xs uppercase tracking-wider text-neutral-400">Artwork</span></header>{item.artwork.imageUrl&&<a href={item.artwork.shopUrl} target="_blank" rel="noreferrer" onClick={()=>trackNetwork("artwork_feed_click",{canonicalProductId:item.artwork.id})}><img src={item.artwork.imageUrl} alt={item.artwork.title} className="max-h-[44rem] w-full rounded-2xl object-cover"/></a>}<h2 className="mt-4 text-xl font-medium">{item.artwork.title}</h2><p className="text-sm text-neutral-500">{item.artwork.offering?.replaceAll("_"," ")} {item.artwork.priceLabel&&`· ${item.artwork.priceLabel}`}</p><footer className="mt-4 flex gap-5 text-sm text-neutral-500"><button onClick={()=>void artworkAction(item.artwork,"like")}>{item.artwork.liked?"Liked":"Like"} {item.artwork.likeCount}</button><button onClick={()=>void artworkAction(item.artwork,"save")}>{item.artwork.saved?"Saved":"Save"}</button><button onClick={()=>navigator.share?.({title:item.artwork.title,url:item.artwork.shopUrl})}>Share</button><Link href={`/profile/${item.artist.slug}`}>Artist</Link><a href={item.artwork.shopUrl} target="_blank" rel="noreferrer">Shop</a></footer></article>)}
{!createOnly&&next&&<button disabled={loading} onClick={()=>void load(next)} className="w-full rounded-full border py-3 text-sm disabled:opacity-50">{loading?"Loading…":"Load more"}</button>}</div>}
