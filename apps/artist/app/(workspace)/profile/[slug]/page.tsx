import { notFound } from "next/navigation";import { ProfileView } from "@/components/network/ProfileView";import { requireNetworkContext,serializeNetworkProfile } from "@/lib/server/network-context";import { connectionState } from "@/lib/server/network-service";import { ConnectionModel,NetworkEventModel,NetworkPostModel,NetworkProfileModel } from "@/lib/server/models";
export const dynamic="force-dynamic";export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;return{title:`${slug} | ARTCLUB Network`,description:"ARTCLUB Network profile"}}
export default async function NetworkProfilePage({params}:{params:Promise<{slug:string}>}){const context=await requireNetworkContext();const{slug}=await params;const profile=await NetworkProfileModel.findOne({slug:slug.toLowerCase(),isPublic:true,suspendedAt:{$exists:false}}).lean();if(!profile)notFound();const[relation,connections,posts,events]=await Promise.all([connectionState(context.profile!._id,profile._id),ConnectionModel.countDocuments({status:"accepted",$or:[{requesterProfileId:profile._id},{recipientProfileId:profile._id}]}),NetworkPostModel.countDocuments({authorProfileId:profile._id,status:"published",visibility:"public"}),NetworkEventModel.countDocuments({organizerProfileId:profile._id,status:"published"})]);return <ProfileView profile={serializeNetworkProfile(profile)} counts={{connections,posts,events}} mine={String(profile._id)===String(context.profile!._id)} relation={relation?{id:relation._id.toString(),status:relation.status}:null}/>}
