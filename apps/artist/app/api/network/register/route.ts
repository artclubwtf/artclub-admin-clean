import { hash } from "bcryptjs";import { networkRegistrationInputSchema } from "@artclub/models";import { connectMongo } from "@/lib/server/mongodb";import { apiError } from "@/lib/server/network-service";import { UserModel } from "@/lib/server/models";import { normalizeShopDomain } from "@/lib/server/shop-domain";
export async function POST(req:Request){const parsed=networkRegistrationInputSchema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return apiError("invalid_registration",400,parsed.error.flatten());const shopDomain=normalizeShopDomain(process.env.SHOPIFY_SHOP_DOMAIN||process.env.NEXT_PUBLIC_SHOPIFY_SHOP_DOMAIN||"");if(!shopDomain)return apiError("network_registration_setup_required",503);await connectMongo();if(await UserModel.exists({email:parsed.data.email}))return apiError("email_already_registered",409);const recent=await UserModel.countDocuments({accountSource:"self_registered",createdAt:{$gte:new Date(Date.now()-60_000)}});if(recent>30)return apiError("rate_limited",429);try{const user=await UserModel.create({email:parsed.data.email,name:parsed.data.name,passwordHash:await hash(parsed.data.password,12),role:"customer",shopDomain,accountSource:"self_registered",onboardingComplete:true,isActive:true});return Response.json({ok:true,userId:user._id.toString()},{status:201})}catch(error:any){if(error?.code===11000)return apiError("email_already_registered",409);throw error}}
