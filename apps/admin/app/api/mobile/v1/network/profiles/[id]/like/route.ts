import { Types } from "mongoose";
import { mobileError, mobileNetworkContext } from "@/lib/mobileNetwork";
import { NetworkNotificationModel, NetworkProfileLikeModel, NetworkProfileModel } from "@/models/Network";
export async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) { const auth = await mobileNetworkContext(req); if (!auth.ok) return auth.response; const { id } = await params; if (!Types.ObjectId.isValid(id) || id === String(auth.profile!._id) || !(await NetworkProfileModel.exists({ _id: id }))) return mobileError("invalid_profile", 404); await NetworkProfileLikeModel.updateOne({ likerProfileId: auth.profile!._id, likedProfileId: id }, { $setOnInsert: { likerProfileId: auth.profile!._id, likedProfileId: id } }, { upsert: true }); await NetworkNotificationModel.create({ recipientProfileId: id, actorProfileId: auth.profile!._id, type: "profile_like", targetType: "profile", targetId: auth.profile!._id }); return Response.json({ ok: true, liked: true }); }
export async function DELETE(req: Request, context: { params: Promise<{ id: string }> }) { const auth = await mobileNetworkContext(req); if (!auth.ok) return auth.response; const { id } = await context.params; await NetworkProfileLikeModel.deleteOne({ likerProfileId: auth.profile!._id, likedProfileId: id }); return Response.json({ ok: true, liked: false }); }
