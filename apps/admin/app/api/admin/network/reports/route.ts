import { requireAdmin } from "@/lib/requireAdmin";import { connectMongo } from "@/lib/mongodb";import { NetworkReportModel } from "@/models/Network";export async function GET(req:Request){const denied=await requireAdmin(req);if(denied)return denied;await connectMongo();const items=await NetworkReportModel.find({status:{$in:["open","reviewing"]}}).sort({createdAt:-1}).limit(200).populate("reporterProfileId","displayName username slug").lean();return Response.json({ok:true,reports:items.map((item:any)=>({id:item._id.toString(),targetType:item.targetType,targetId:item.targetId.toString(),reason:item.reason,details:item.details||"",status:item.status,createdAt:item.createdAt,reporter:item.reporterProfileId?{displayName:item.reporterProfileId.displayName,username:item.reporterProfileId.username}:null}))})}
